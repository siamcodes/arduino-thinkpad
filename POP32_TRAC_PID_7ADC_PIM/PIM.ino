void PIM() {
  Forward(Speed, 200);
  TracTime(Speed, 1000);     //เริ่ม
  TracTime2(60, 10000);   //ใหญ่
  TracTime(50, 10000);       //เล็ก
  TracTime(50, 3000);       //ก่อนตะเกียบ
  TracTime(Speed, 15000);
 TracTime(Speed, 15000);
}
