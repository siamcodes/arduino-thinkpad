void m1()
{
  // FFtimer(baseSpeed, Kp, Kd, Time, maxSpeed, minSpeed);
  //  FF(30, 0.11, 0.05, 'p', 50, 50, -10);
  //  SpinL();
  FFtimer(30, 0.11, 0.05, 1000, 70, -20);
  FFtimer(30, 0.15, 0.15, 1000, 70, -20);
  // FF(60, 0.03, 0.05, 'p', 150, 100, -10);
  // FF(50,0.03,0.05,'p',150,100,-10);
  // SpinL();
  // FF(50,0.03,0.05,'p',150,100,-10);

}
